// Export function
export function add(a, b) {
  return a + b;
}

// Export variabel
export const pi = 3.14;

// Export default (hanya 1 per file)
export default function multiply(a, b) {
  return a * b;
}
